<!DOCTYPE html>
<img src="{{asset('images/on_the_way_to_meet_mom.png')}}" alt="" width="400" height="600" class="comic-cover">

{{-- @props(['src', 'title'])

<img
    src="{{ $src }}"
    alt="{{ $title }}"
    width="400" height="600" class="comic-cover""
/> --}}